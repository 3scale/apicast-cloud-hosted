apiVersion: v1
kind: ImageStream
metadata:
  name: apicast
  labels:
    app: apicast
spec:
  tags:
  - name: builder
    from:
      kind: DockerImage
      name: quay.io/3scale/apicast:master-builder
